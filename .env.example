# Life RPG — public env template (no secrets)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Optional: used only if your schema/API needs service-role on the server.
# Never prefix a secret with NEXT_PUBLIC_ and never commit real values.
# SUPABASE_SECRET_KEY=
